# =============================================================================
# Build Environment Variables
# Copy to .build.env and fill in your values
# Used by build scripts for frontend build-time arguments
# =============================================================================

# Production Domain
# =================
# Set your production domain (without protocol)
# Examples: intell.cedia.org.ec, intell.cedia.org.ec
PRODUCTION_DOMAIN=intell.cedia.org.ec

# Frontend Build-Time Variables
# ==============================
# These are baked into the Next.js frontend during Docker build
# URLs are automatically constructed from PRODUCTION_DOMAIN above
# You can override them manually if needed
# NEXT_PUBLIC_API_BASE_URL=https://intell.cedia.org.ec/api
# NEXT_PUBLIC_WS_BASE_URL=wss://intell.cedia.org.ec/ws
NEXT_PUBLIC_APP_ENV=production
