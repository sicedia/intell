# =============================================================================
# Docker Compose Environment Variables
# Copy to .docker.env and fill in your values
# Used by docker-compose.prod.yml for service configuration
# =============================================================================

# PostgreSQL Database Configuration
# ===================================
POSTGRES_DB=intell
POSTGRES_USER=intell_user
# IMPORTANT: Change this to a strong password in production!
POSTGRES_PASSWORD=change-this-strong-password-in-production

# Redis Configuration (Optional)
# ===============================
# If you want to password-protect Redis, uncomment and set:
# REDIS_PASSWORD=your-redis-password

# Docker Image Configuration
# ==========================
# Tag version of Docker images to pull from Docker Hub
# Examples: latest, v1.0.0, v1.0.1, etc.
IMAGE_TAG=latest
