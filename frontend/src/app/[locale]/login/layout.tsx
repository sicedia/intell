/**
 * Login page layout.
 *
 * Minimal layout without AppShell (no sidebar, topbar, etc.)
 */
export default function LoginLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
