# =============================================================================
# Next.js Frontend - Production Environment Variables
# These are baked into the frontend during Docker build
# Set in infrastructure/.env for Docker Compose
# =============================================================================

# API Base URL
# Production: Your production domain
NEXT_PUBLIC_API_BASE_URL=https://openintell.cedia.org.ec/api

# WebSocket Base URL
# Production: Your production domain with WSS (secure WebSocket)
NEXT_PUBLIC_WS_BASE_URL=wss://openintell.cedia.org.ec/ws

# Application Environment
# Options: development, staging, production
NEXT_PUBLIC_APP_ENV=production
